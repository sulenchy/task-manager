import React from 'react'
import ReactDOM from 'react-dom'

import App from './components/App';

// TODO: create another branch and convert the language to ts(typescript)


ReactDOM.render(<App />, document.getElementById("root"));